addons:
  apt:
    packages:
    - libwxgtk2.8-dev
    
language: cpp

compiler:  
    - gcc

before_script:  
    - cd ~
    - cd $TRAVIS_BUILD_DIR
    - mkdir build
    - cd build
    - cmake ..

script: make 
